/**
 * Strategies for handling each parameter. As more versions and platforms (OS and architecture) variations are supported, specific logic is expected to increase. Each strategy has access to all parameter values to accommodate cross-cutting concerns, and it's not assumed each parameter will emit only one argument.
 *
 * @author [[mailto:michael@ahlers.consulting Michael Ahlers]]
 */
package ahlers.phantom.embedded.parameters;
